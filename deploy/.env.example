# Copier vers deploy/.env et adapter.
# HTTPS (Let's Encrypt) : même hostname que le DNS du VPS.
ORI_CADDY_ADDRESS=scan.votredomaine.tld

# Optionnel
# ORI_ENABLE_RDAP=1
# ORI_WEBHOOK_URL=https://example.com/webhooks/scan
# ORI_DIGEST_WEBHOOK_URL=https://example.com/webhooks/digest
