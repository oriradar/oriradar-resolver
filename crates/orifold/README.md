# orifold

**Fold** domain strings into typosquat variants — Rust library (open source).

- Metaphor: origami folds transform a flat sheet; here we transform a brand domain into candidate variants.
- Consumed by [**oritypo-solver**](../../backend/) (Python) via subprocess/JSON lines or FFI (TBD).

```bash
cargo test
```
